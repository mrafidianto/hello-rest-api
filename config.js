/**
 * Configuration variable to start the server
 */

// config variable environment
const config = {
  'httpPort' : 3000,
  'httpsPort' : 3001,
  'envName' : "development"
}

module.exports = config